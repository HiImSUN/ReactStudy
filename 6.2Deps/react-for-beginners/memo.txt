상황을 생각해보자.
검색기능이 있고 countup버튼이 있다고 했을 때
검색어를 입력할 때마다 검색을 해주는 것은 좋지만,
countup버튼을 누를 때마다 검색을 실행하는 것은 비효율적이다.
따라서 우리는 이 둘을 분리해서 개별동작 시킬 것이다.

useEffect(A, B); 에서
A는 한 번만 실행된다고 배웠었는데,
사실 B의 []가 빈 리스트라 그렇다.
빈 리스트에 [keyword] 가 있다면 keyword 값이 변경될 때마다
A의 코드가 실행되는 것이다.